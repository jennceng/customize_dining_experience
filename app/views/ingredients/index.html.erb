<h1>Ingredients:</h1>

<%= bar_chart @total_stats, stacked: true %> 

<ul>
  <% @ingredients.each do |ingredient| %>
    <li> <%= link_to ingredient.name, ingredient_path(ingredient) %> </li>
  <% end %>
</ul>

<%= link_to 'Add New Ingredient', new_ingredient_path %>
